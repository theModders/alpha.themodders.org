{
	"name": "alpha.themodders.org",
	"author": "Adam Golebiowski <themodders@avallach.ovh>",
	"private": true,
	"engines": {
		"node": "10"
	},
	"dependencies": {
		"firebase-admin": "~8.10.0",
		"firebase-functions": "^3.6.2"
	},
	"devDependencies": {
		"@babel/cli": "^7.10.1",
		"@babel/core": "^7.10.1",
		"@babel/preset-env": "^7.10.1",
		"firebase-tools": "^8.4.1"
	},
	"scripts": {
		"start": "npm run build && firebase emulators:start --inspect-functions",
		"clean": "rm -rf ../.build/server",
		"build": "npm run clean && npm install && babel . --out-dir ../.build/server --source-maps --copy-files --ignore node_modules && cd ../.build/server && npm install",
		"deploy": "npm run build && cd .. && firebase deploy"
	}
}
